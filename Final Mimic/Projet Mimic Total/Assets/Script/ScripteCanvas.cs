using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ScripteCanvas : MonoBehaviour
{
    public Text texte1;
    // Start is called before the first frame update
    void Start()
    {
        texte1.enabled = true;
        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
